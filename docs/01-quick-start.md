# Глава 1: Быстрый старт 🚀

> Всё, что нужно для запуска проекта за 5 минут

---

## 📋 Требования

Перед началом работы убедитесь, что у вас установлены:

- **Node.js** — версия 18.x или выше
- **npm** — версия 9.x или выше
- **Git** — для клонирования репозитория

### Проверка версий

```bash
node --version   # v18.x.x или выше
npm --version    # 9.x.x или выше
git --version    # 2.x.x
```

---

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd it-party
```

### 2. Установка зависимостей

```bash
npm install
```

Это установит все необходимые пакеты из `package.json`:

- Next.js, React, React DOM (фреймворк)
- TypeScript (типизация)
- SCSS (стилизация)
- clsx (работа с классами)
- dayjs, lodash-es (утилиты)

---

## ⚙️ Конфигурация

### Переменные окружения (.env.local)

Создайте файл `.env.local` в корне проекта (если требуется):

```bash
# Опциональные параметры
NEXT_PUBLIC_API_URL=https://api.example.com/api
NODE_ENV=development
```

> ⚠️ **Важно**: Файл `.env.local` добавлен в `.gitignore` и не коммитится в репозиторий!

---

## 🚀 Запуск проекта

### Режим разработки

```bash
npm run dev
```

Проект будет доступен по адресу: `http://localhost:3000`

### Production сборка

```bash
npm run build
npm run start
```

---

## 📁 Структура проекта

```
it-party/
├── src/
│   ├── app/              # Next.js App Router
│   ├── widgets/          # Виджеты
│   ├── features/         # Функциональность
│   ├── entities/         # Бизнес-сущности
│   └── shared/           # Переиспользуемый код
├── docs/                 # Документация
├── public/               # Статические файлы
├── next.config.ts        # Конфигурация Next.js
└── tsconfig.json         # Конфигурация TypeScript
```

---

## 🎯 Следующие шаги

После успешного запуска:

1. Откройте `http://localhost:3000` в браузере
2. Изучите [Архитектурные решения](./02-architecture.md)
3. Прочитайте [FSD Принципы](./03-fsd-principles.md)
4. Изучите [Работу с API](./05-api-guide.md)

---

## ❓ Решение проблем

### Порт уже занят

```bash
# Используйте другой порт
PORT=3001 npm run dev
```

### Ошибки TypeScript

```bash
# Проверка типов
npm run type-check
```

### Очистка кеша

```bash
# Удаление .next папки
rm -rf .next
npm run dev
```

---

**Далее**: [Глава 2: Архитектурные решения](./02-architecture.md) 🏗️
